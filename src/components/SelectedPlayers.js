import React from 'react';
import { Typography } from '@mui/material';

const Selected = () => {
  // Replace this with actual logic to fetch/display favorite players
  return (
    <div>
      <Typography variant="h6">Selected Matches</Typography>
      <Typography>
        Feature to display favorite players coming soon!
      </Typography>
    </div>
  );
};

export default FavouritePlayers;
